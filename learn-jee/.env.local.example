# Supabase Configuration for Learn JEE
# 
# IMPORTANT: Use the SAME Supabase project as the main iiskills.cloud app
# This enables cross-subdomain authentication and shared user data.
# 
# To set up:
# 1. Copy this file to .env.local
# 2. Use the same values from the main app's .env.local
# 3. Never commit .env.local to git (it's in .gitignore)

# Your Supabase project URL (same as main app)
# Example: https://xyzcompany.supabase.co
NEXT_PUBLIC_SUPABASE_URL=your-project-url-here

# Your Supabase anonymous/public key (same as main app)
# This key is safe to use in the browser
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Site URL for OAuth redirects
# Production: https://learn-jee.iiskills.cloud
# Development: http://localhost:3010
NEXT_PUBLIC_SITE_URL=http://localhost:3010

# Cookie domain for cross-subdomain sessions (production only)
# Set this to .iiskills.cloud in production
# Leave empty for localhost development
NEXT_PUBLIC_COOKIE_DOMAIN=

# ============================================
# Universal reCAPTCHA Configuration
# ============================================
#
# IMPORTANT: Use the SAME reCAPTCHA keys across ALL apps and subdomains
# This enables unified bot protection for newsletter signups, registration, 
# and authentication across the entire iiskills.cloud platform.
#
# To set up:
# 1. Register a SINGLE site key for iiskills.cloud at:
#    https://www.google.com/recaptcha/admin/create
# 2. Choose reCAPTCHA v3 for invisible verification
# 3. Add iiskills.cloud as the domain (covers all subdomains)
# 4. Use the SAME keys in ALL apps' .env.local files
#
# Public site key (visible in browser, same across all apps)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your-recaptcha-site-key-here

# Secret key (server-side only, never exposed to browser, same across all apps)
# ⚠️ IMPORTANT: Keep this secret!
RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key-here

