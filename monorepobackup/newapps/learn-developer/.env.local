# ===========================================
# SANDBOX ENVIRONMENT - auth/paywall disabled
# ===========================================
# This file is committed intentionally.
# It contains only placeholder values — no real credentials.

# Supabase — default: suspended (mock client, no real DB connection)
# To use a real sandbox Supabase project instead:
#   1. Export the following vars in your shell BEFORE building and starting PM2:
#        export SANDBOX_SUPABASE_URL=https://<ref>.supabase.co
#        export SANDBOX_SUPABASE_ANON_KEY=<anon-key>
#        export SANDBOX_SUPABASE_SERVICE_ROLE_KEY=<service-role-key>
#   2. Run: yarn sandbox:build   (shell env overrides .env.local placeholders)
#   3. Run: pm2 start ecosystem.newapps.config.js
#   See newapps/README.md → "Connecting to a sandbox Supabase project" for full instructions.
NEXT_PUBLIC_SUPABASE_SUSPENDED=true
NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sandbox-placeholder-anon-key-longer-than-20-chars
SUPABASE_SERVICE_ROLE_KEY=sandbox-placeholder-service-role-key

# Disable all authentication and paywalls
NEXT_PUBLIC_DISABLE_AUTH=true
NEXT_PUBLIC_TESTING_MODE=true

# Open access mode (bypasses paywall UI)
NEXT_PUBLIC_OPEN_ACCESS=true

# Site URL
NEXT_PUBLIC_SITE_URL=http://app1.iiskills.cloud
