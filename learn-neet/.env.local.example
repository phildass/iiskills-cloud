# Supabase Configuration for Learn-NEET
#
# IMPORTANT: Use the SAME Supabase credentials as the main app
# All apps must share the same Supabase project for universal authentication
#
# For Google OAuth setup, see ../GOOGLE_OAUTH_TROUBLESHOOTING.md

# Your Supabase project URL
# MUST be the same as the main app's .env.local
# Example: https://xyzcompany.supabase.co
NEXT_PUBLIC_SUPABASE_URL=your-project-url-here

# Your Supabase anonymous/public key
# MUST be the same as the main app's .env.local
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Site URL for OAuth redirects
# Development: http://localhost:3013 (learn-neet development port)
# Production: https://learn-neet.iiskills.cloud
# IMPORTANT: This must match one of the redirect URLs configured in Supabase
NEXT_PUBLIC_SITE_URL=http://localhost:3013

# Cookie domain for cross-subdomain authentication
# Development: Leave empty or undefined (cookies work on localhost)
# Production: .iiskills.cloud (note the leading dot - enables cross-subdomain auth)
NEXT_PUBLIC_COOKIE_DOMAIN=

# For production deployment, set:
# NEXT_PUBLIC_COOKIE_DOMAIN=.iiskills.cloud
# NEXT_PUBLIC_SITE_URL=https://learn-neet.iiskills.cloud

# ============================================
# Universal reCAPTCHA Configuration
# ============================================
#
# IMPORTANT: Use the SAME reCAPTCHA keys across ALL apps and subdomains
# This enables unified bot protection across the entire iiskills.cloud platform.
#
# CURRENT USAGE: Newsletter signups 
# FUTURE USAGE: Registration, authentication, login, password reset forms
#
# To set up:
# 1. Register a SINGLE site key for iiskills.cloud at:
#    https://www.google.com/recaptcha/admin/create
# 2. Choose reCAPTCHA v3 for invisible verification
# 3. Add iiskills.cloud as the domain (covers all subdomains)
# 4. Use the SAME keys in ALL apps' .env.local files
#
# Public site key (visible in browser, same across all apps)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your-recaptcha-site-key-here

# Secret key (server-side only, never exposed to browser, same across all apps)
# ⚠️ IMPORTANT: Keep this secret!
RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key-here

# ============================================
# Google OAuth Configuration Notes
# ============================================
# 
# Google OAuth is configured at the Supabase project level (shared across all apps)
# You don't need separate Google OAuth credentials for this subdomain
#
# To enable Google sign-in:
# 1. Ensure Google provider is enabled in Supabase Dashboard
# 2. Add this subdomain's URLs to Supabase redirect URLs:
#    - Development: http://localhost:3013/**
#    - Production: https://learn-neet.iiskills.cloud/**
# 3. Add to Google Cloud Console JavaScript origins:
#    - Development: http://localhost:3013
#    - Production: https://learn-neet.iiskills.cloud
#
# For detailed instructions:
# - See ../GOOGLE_OAUTH_TROUBLESHOOTING.md
# - See ../CALLBACK_URLS_REFERENCE.md for complete URL list
# - Run: cd .. && ./google-oauth-check.sh
